# ProGuard rules go here
# This file is only used if minifyEnabled true in build.gradle.
# Add -keep rules here for libraries that use reflection or won't work with shrinking/obfuscation.
# Example:
# -keep class com.yourpackage.** { *; }